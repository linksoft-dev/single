import 'package:flutter/cupertino.dart';
import 'package:single/forms/CrudFormDetail.dart';

class ContasForm extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return CrudFormDetail(
      screenName: "Contas",
      body: Container(),
    );
  }
}
